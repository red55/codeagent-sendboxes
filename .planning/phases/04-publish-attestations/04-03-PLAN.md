---
phase: 04-publish-attestations
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [README.md]
autonomous: true
requirements: [REQ-NFR-003]
user_setup: []
must_haves:
  truths:
    - "README documents CI/CD workflows and their triggers"
    - "README explains how to view scan results"
    - "README documents how to trigger manual builds"
  artifacts:
    - path: "README.md"
      provides: "Minimal CI/CD documentation for the sandbox pipeline"
      min_lines: 30
  key_links:
    - from: "README.md"
      to: ".github/workflows/pr.yml"
      via: "references PR build workflow and fork PR push behavior"
    - from: "README.md"
      to: ".github/workflows/release.yml"
      via: "references release workflow with SBOM + provenance"
---

<objective>
Create minimal README.md documenting the CI/CD pipeline — how to trigger builds, view scan results, and understand the workflows.

Purpose: Fulfill REQ-NFR-003 (maintainability — README updated with CI/CD documentation). Per D-10, keep documentation minimal: only how to trigger builds and view results.

Output: `README.md` with CI/CD section covering workflow triggers, scan result locations, and manual build instructions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-publish-attestations/04-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# File to CREATE:
# README.md — minimal CI/CD documentation

# Decisions to implement:
# D-10: Minimal README documentation — only how to trigger builds and view results

# Workflows to document:
# pr.yml — runs on PRs to main, fork PRs push to ghcr.io with pr-<number> tags
# release.yml — runs on push to main and v* tags, pushes with SBOM + provenance
# trivy-cron.yml — daily Trivy DB pre-warming (optional documentation)

# Key documentation points:
# 1. What workflows exist and when they trigger
# 2. Where to view scan results (GitHub Security tab)
# 3. How to trigger manual builds (workflow_dispatch)
# 4. Fork PR behavior (pushes to registry)
# 5. Release behavior (SBOM + provenance on v* tags)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with minimal CI/CD documentation</name>
  <files>README.md</files>
  <read_first>
    .planning/phases/04-publish-attestations/04-CONTEXT.md (D-10)
    .planning/ROADMAP.md (Phase 4 overview)
  </read_first>
  <acceptance_criteria>
    - File `README.md` exists
    - File has CI/CD section documenting workflows
    - Documents pr.yml (PR builds, fork PR push behavior)
    - Documents release.yml (release builds, SBOM + provenance)
    - Documents how to view scan results (GitHub Security tab)
    - Documents manual build trigger (workflow_dispatch)
    - Content is minimal (per D-10) — no full CI/CD guide
    - File has at least 30 lines
  </acceptance_criteria>
  <action>
Create `README.md` with minimal CI/CD documentation:

```markdown
# Sandbox CI/CD Pipeline

AI agent sandbox Docker images with automated linting, building, security scanning, and publishing.

## CI/CD Workflows

### PR Build (`pr.yml`)
- **Triggers:** Pull requests to `main` branch
- **Jobs:** Lint → Build (4 variants) → Trivy Security Scan
- **Fork PRs:** Images pushed to `ghcr.io/red55/sandbox-<variant>:pr-<number>` for review
- **Non-fork PRs:** Images built locally for scanning only (no registry push)

### Release (`release.yml`)
- **Triggers:** Push to `main`, `v*` tags, manual dispatch
- **Jobs:** Lint → Build (4 variants with SBOM + provenance) → Trivy Security Scan
- **Images:** Pushed to `ghcr.io/red55/sandbox-<variant>` with SHA, branch, and semver tags
- **Attestations:** CycloneDX SBOM + build provenance (mode=max) on all release images

### Trivy Cron (`trivy-cron.yml`)
- **Schedule:** Daily at 06:00 UTC
- **Purpose:** Pre-warm Trivy vulnerability database to avoid rate limits

## Viewing Results

- **Lint results:** PR comments (actionlint) and GitHub Security tab (hadolint SARIF)
- **Security scans:** GitHub Security tab → Code scanning alerts (Trivy SARIF uploads)
- **PR comments:** Trivy findings posted as PR review comments
- **Build artifacts:** Fork PR images available at `ghcr.io/red55/sandbox-<variant>:pr-<number>`

## Manual Builds

Trigger any workflow manually via **Actions** tab → select workflow → **Run workflow**.
Or use `workflow_dispatch` — release workflow supports manual triggering.

## Image Variants

| Variant | Base | Tools |
|---------|------|-------|
| `sandbox-oc-go` | OpenCode + Go | Go tooling, OpenCode agent |
| `sandbox-qc-go` | QWEN Code + Go | Go tooling, QWEN Code agent |
| `sandbox-oc-ansible` | OpenCode + Ansible | Ansible tooling, OpenCode agent |
| `sandbox-qc-ansible` | QWEN Code + Ansible | Ansible tooling, QWEN Code agent |
```

Key implementation details:
- Per D-10: Minimal documentation — only triggers, result viewing, and manual builds
- No full CI/CD guide (out of scope)
- No environment protection rules (not needed for this project scale)
- No image signing documentation (cosign deferred to future phase)
- SBOM and provenance mentioned in release section (D-01, D-03)
- Fork PR push behavior explained (D-05, D-06)
  </action>
  <verify>
    <automated>wc -l README.md | awk '{print ($1 >= 30) ? "MIN_LINES_MET" : "BELOW_MIN"}'</automated>
    <automated>grep -c "CI/CD" README.md && echo "HAS_CI_CD_SECTION" || echo "MISSING_CI_CD_SECTION"</automated>
    <automated>grep -c "Security tab" README.md && echo "HAS_SCAN_RESULTS" || echo "MISSING_SCAN_RESULTS"</automated>
    <automated>grep -c "workflow_dispatch\|Manual" README.md && echo "HAS_MANUAL_TRIGGER" || echo "MISSING_MANUAL_TRIGGER"</automated>
    <automated>grep -c "fork" README.md && echo "HAS_FORK_DOC" || echo "MISSING_FORK_DOC"</automated>
  </verify>
  <done>
    `README.md` created with minimal CI/CD documentation: workflow triggers, scan result locations, manual build instructions, and image variant table. Per D-10, scope limited to usage documentation only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → External readers | Documentation is public and may reveal internal architecture |
| README → Workflow references | Links to workflows should not expose sensitive configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | README reveals workflow structure | accept | README documents public CI/CD configuration. No secrets or sensitive info included. |
| T-04-10 | Tampering | README content | mitigate | Source-controlled, reviewed via PR like any other documentation. |
</threat_model>

<verification>
1. Verify README exists: `test -f README.md && echo "EXISTS" || echo "MISSING"`
2. Verify minimum line count: `wc -l README.md` (must be >= 30)
3. Verify CI/CD section present: `grep "CI/CD" README.md`
4. Verify scan results documentation: `grep "Security tab" README.md`
5. Verify manual trigger documentation: `grep -i "manual\|workflow_dispatch" README.md`
6. Verify fork PR documentation: `grep -i "fork" README.md`
</verification>

<success_criteria>
- `README.md` exists with >= 30 lines
- CI/CD section documents pr.yml and release.yml workflows
- Fork PR push behavior explained (D-05, D-06)
- Scan result location documented (GitHub Security tab)
- Manual build trigger documented (workflow_dispatch)
- Image variants table included
- Content is minimal — no full CI/CD guide (per D-10)
- No documentation of deferred items (cosign signing, environment protection rules)
- REQ-NFR-003 covered: README updated with CI/CD documentation
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish-attestations/04-03-SUMMARY.md`
</output>
