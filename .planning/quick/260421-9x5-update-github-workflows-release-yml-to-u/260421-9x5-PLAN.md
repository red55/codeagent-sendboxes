---
phase: quick-fix
plan: 9x5
type: execute
wave: 1
depends_on: []
files_modified: [.github/workflows/release.yml]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "release.yml runs actionlint via reviewdog/action-actionlint@v1"
    - "The old rhysd/actionlint@v1 step is removed"
  artifacts:
    - path: .github/workflows/release.yml
      provides: "Updated release pipeline with reviewdog actionlint"
  key_links:
    - from: .github/workflows/release.yml
      to: reviewdog/action-actionlint@v1
      via: "uses: reviewdog/action-actionlint@v1"
      pattern: "reviewdog/action-actionlint@v1"
---

<objective>
Replace `rhysd/actionlint@v1` with `reviewdog/action-actionlint@v1` in the release.yml workflow.

Purpose: Use the more capable reviewdog-backed actionlint action that can post results as PR comments when applicable. Since release.yml triggers on push/tag (not PR), comment posting won't produce output, but the action still runs actionlint correctly.

Output: Updated .github/workflows/release.yml with reviewdog/action-actionlint@v1
</objective>

<execution_context>
@/home/agent/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.github/workflows/release.yml

# Current state: Lines 68-71 use rhysd/actionlint@v1 with config-file parameter
# Target: Replace with reviewdog/action-actionlint@v1

# Per Context7 docs, reviewdog/action-actionlint@v1 supports:
#   - reporter: github-pr-review, github-check, etc.
#   - fail_level: error, warning (defaults to error)
#   - github_token: for PR comment posting
# It reads config from .github/lint/.actionlint.yaml automatically (no config-file param needed)
</context>

<tasks>

<task type="auto">
  <name>Replace rhysd/actionlint@v1 with reviewdog/action-actionlint@v1</name>
  <files>.github/workflows/release.yml</files>
  <action>Replace the existing actionlint step (lines 68-71) with reviewdog/action-actionlint@v1. The new step should include:
- `uses: reviewdog/action-actionlint@v1`
- `reporter: github-pr-review` (enables PR comment posting when triggered on PRs)
- `fail_level: error` (fails the job on errors)
- `github_token: ${{ secrets.GITHUB_TOKEN }}` (required for PR comment posting)

The `config-file` parameter is NOT needed — reviewdog/action-actionlint reads from `.github/lint/.actionlint.yaml` automatically via the standard actionlint config discovery.

Before:
```yaml
      - name: Run actionlint
        uses: rhysd/actionlint@v1
        with:
          config-file: .github/lint/.actionlint.yaml
```

After:
```yaml
      - name: Run actionlint
        uses: reviewdog/action-actionlint@v1
        with:
          reporter: github-pr-review
          fail_level: error
          github_token: ${{ secrets.GITHUB_TOKEN }}
```
</action>
  <verify>grep -n "reviewdog/action-actionlint@v1" .github/workflows/release.yml && ! grep -n "rhysd/actionlint" .github/workflows/release.yml</verify>
  <done>release.yml uses reviewdog/action-actionlint@v1 with reporter, fail_level, and github_token. The rhysd/actionlint reference is fully removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow→GITHUB_TOKEN | Reviewdog action uses GITHUB_TOKEN for PR comment posting — standard pattern, no escalation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Elevation of Privilege | reviewdog/action-actionlint | accept | Uses GITHUB_TOKEN (standard GitHub Actions token, limited scope). No secrets exposure. |
</threat_model>

<verification>
1. Verify YAML syntax: `actionlint .github/workflows/release.yml` (should pass with no errors)
2. Verify the step is correctly placed in the lint job (after hadolint steps, before build job depends on lint)
</verification>

<success_criteria>
- `rhysd/actionlint@v1` is fully removed from release.yml
- `reviewdog/action-actionlint@v1` is present with reporter, fail_level, and github_token
- actionlint validates the workflow file passes (no syntax errors introduced)
</success_criteria>

<output>
After completion, update .github/workflows/release.yml directly. No summary file needed for quick fixes.
</output>
